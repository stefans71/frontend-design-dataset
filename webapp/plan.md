# Webapp Redesign Plan

## Current Site Map

```
qwen.data-analytics.space
├── / (Home)
│   ├── Hero — model name, training stats, download buttons
│   ├── Stats row — qualifying questions 1/10→10/10, +1.0 delta, 3090 records, Apache 2.0
│   ├── How It Works — 3-step cards (Generate → Critique & Improve → Fine-Tune)
│   ├── Before/After — base vs fine-tuned comparison image
│   ├── Featured — 6 top-scored training components
│   └── Download — 8B Expert + 4B Lite model cards
│
├── /validation (Validation)
│   ├── Header — Qwen3-VL-8B BASE vs FINE-TUNED
│   ├── Stats — 4.5 base avg, 5.5 fine-tuned avg, +1.0 delta, 10 tested
│   └── Results table — 10 rows, expandable with side-by-side screenshots + critique
│
├── /components (Gallery → rename to "Training Data")
│   ├── Header — "Components", generated by Qwen3.6-27B, critiqued by GPT-5.4
│   ├── Filter sidebar — category, theme, score range, sort
│   ├── Grid — 24 per page, 3-col desktop / 1-col mobile
│   └── Pagination
│
├── /components/:id (Component Detail)
│   ├── Tabs — Original / Critique / Improved
│   ├── Screenshot + rendered HTML
│   └── Metadata footer
│
└── /conversations (Conversations)
    ├── Header — 254 traces, 150 qualifying / 104 immediate
    ├── Filters — type (all/qualifying/immediate), domain
    └── Conversation list — expandable cards
```

**Navbar:** FDE | Home | Validation | Gallery | Conversations | [A- A+] [theme]

## TODO

### Phase 1 — Training Data Page Improvements

- [ ] **1.1 Rename nav link** — "Gallery" → "Training Data" in Navbar.tsx
- [ ] **1.2 Add component ID sort** — new `id_asc` / `id_desc` sort option
  - Add to `SortBy` type in `src/lib/types.ts`
  - Add to FilterSidebar sort dropdown
  - Handle in server `ORDER BY` clause in `server/index.ts`
- [ ] **1.3 Show component # on card** — display component ID on ComponentCard
  - Below the score badge or in the metadata footer
  - Format: short ID like `#001-r3` (from `component-001-run3`)
- [ ] **1.4 Test & verify** — check all viewports, push, verify deploy

### Phase 2 — Navigation Restructure

- [ ] **2.1 Design nav dropdown** — nest Validation, Training Data, Conversations under main menu
- [ ] **2.2 Rename "Home" → "Fine Tuned"** — becomes the parent/dropdown label
- [ ] **2.3 Implement dropdown** — CSS-only or minimal JS, matches Linear aesthetic
- [ ] **2.4 Mobile nav** — ensure dropdown works on touch devices
- [ ] **2.5 Test & verify** — all viewports, push, verify deploy

## Files to Edit

| Task | Files |
|---|---|
| 1.1 Nav rename | `src/components/layout/Navbar.tsx` |
| 1.2 Sort option | `src/lib/types.ts`, `src/components/FilterSidebar.tsx`, `server/index.ts` |
| 1.3 Card ID | `src/components/ComponentCard.tsx` |
| 2.1–2.4 Nav restructure | `src/components/layout/Navbar.tsx`, `src/styles/globals.css` |
